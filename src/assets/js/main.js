import Alpine from "alpinejs";

// Optionally register custom Alpine components and directives here
// Alpine.data('dropdown', () => ({ open: false }))

// Initialize Alpine
window.Alpine = Alpine;
Alpine.start();
